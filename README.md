# ntag-url

NFCタグ向けの、クエリパラメータ表示/生成Webアプリです。

## できること

- URLのクエリパラメータを画面に表示
  - 日本語対応（URLエンコード/デコード）
  - 改行対応（`%0A` など）
  - URL文字列はリンクとして表示
- 項目（キー/値）を入力してURLを生成
- 生成したURLをワンクリックでコピー

## 使い方

`index.html` をブラウザで開くだけで動作します。